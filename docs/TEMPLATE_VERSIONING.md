# Template versioning

Template versions belong to a format family, have immutable positive version numbers and JSON definitions, and support `DRAFT`, `TESTING`, `APPROVED`, `DEPRECATED`, `REJECTED`, and legacy `RETIRED` states. Fields and samples reference a version. Extraction results have an optional template-version slot. Final visual Template Studio behavior is Phase 4 and was not implemented.
