# Legacy template compatibility

Existing certified parser definitions are marked `LEGACY_PARSER`; imported/wrapped definitions may use `LEGACY_ADAPTER`. Template Studio presents a safe schema wrapper and limitation note rather than silently reinterpreting legacy JSON as visual coordinates. Approved legacy versions remain immutable and readable. Cloning creates a DRAFT; replacement requires parity and golden-sample testing.
