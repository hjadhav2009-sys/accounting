# Anchor rules

Anchors store label text, target field, relationship (`RIGHT_OF`, `LEFT_OF`, `BELOW`, `ABOVE`, `NEAREST`, `SAME_LINE`), exact/case/punctuation match policy, page policy and tolerance. Tolerance is capped at 0.20. The label-value shortcut creates the same structured rule and uses no fuzzy model or external service.
