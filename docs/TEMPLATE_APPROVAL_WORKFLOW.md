# Template approval workflow

DRAFT may be edited and TESTING may contain failures. Approval is restricted to OWNER/ADMIN and requires at least one protected sample, a completed latest test run, every result VERIFIED, golden comparisons matching and deterministic validation passing. The approval record stores approver/time, counts, test-run ID, validation profile and schema version. AI has no approval action and there is no casual validation bypass.
