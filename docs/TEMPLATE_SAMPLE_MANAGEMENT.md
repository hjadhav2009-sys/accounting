# Template sample management

Each version may reference multiple tenant/company-scoped Document Library records. Add/remove affects references only and never commits PDF bytes. Expected normalized output and reviewer notes are protected PostgreSQL data. Approved historical versions retain their sample regression evidence. Page-count deviation across three or more samples is marked “Possible outlier” but is not automatically excluded.
